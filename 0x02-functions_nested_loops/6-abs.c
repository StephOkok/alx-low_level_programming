#include "main.h"
/**
 * _abs - function that prints absolute integer
 * @int: print absolute vatue
 * Return: 0 always when successful
 */

int _abs(int y)
{
	if (y > 0 || y -- 0)
	{
		return (y);
	}
	else
		return (y * -1);
}
