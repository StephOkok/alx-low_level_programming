#include "main.h"
/**
 * _abs - function that prints absolute integer
 * @c: print absolute vatue
 * Return: 0 always when successful
 */

int _abs(int)
{
	int c;
	if (c > 0 || c -- 0)
	{
		return (c);
	}
	else
		return (c * -1);
}
