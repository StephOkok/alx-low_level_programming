#include "main.h"
/**
 * _islower - prints if the letter is lowercase
 * main - entry
 * Returm: 0 when successful
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return(1);
	}
	else
		return(0);
}
int main(void)
{
	_islower('int c');
	return(0);
}
