#include <stdio.h>
/**
* main - entry to the code
* main - print alphabets
* Return: 0 meaning success
*/
int main(void)
{
	char alpha;
	for (alpha = 'a'; alpha <= 'z'; alpha++)
		
	{
		putchar (alpha);
	}
putchar ('\n');
return(0);
}
