#include "main.h"
/**
* main - tests function that prints if integer is positive or negative
* Return: 0
* @i: number to check if positive or negative
* positive_or_negative - function to print if postive or negative
*/

int main(void)
{
	int i;
	
	i = 0;
	
	positive_or_negative(i);
	
	return (0);
}
