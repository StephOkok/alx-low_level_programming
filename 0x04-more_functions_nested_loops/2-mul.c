#include "main.h"

/**
 * mul - function to multiply two integers
 * @a: first integer declared for multiplication
 * @b: second integer declared for multiplication
 * Return: Result
 */

int mul(int a, int b)
{
	return (a * b);
}
